import { Container } from "react-bootstrap";
const Home = () => {
  return (
    <Container
      fluid
      style={{ backgroundColor: "#9FA6B2", height: "100vh" }}
      className="h-100"
    >
      <header>Memory API Demo (React frontend & C# backend)</header>
    </Container>
  );
};

export default Home;
